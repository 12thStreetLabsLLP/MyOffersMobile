import React from "react";
import ContentView from "../../layouts/auth/otp-verification";

export const OTPVerificationScreen = ({
  navigation,
  route,
}): React.ReactElement => <ContentView navigation={navigation} route={route} />;
