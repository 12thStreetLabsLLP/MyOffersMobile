import React from "react";
import ContentView from "../../layouts/auth/update-user";

export const UpdateProfileScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation} />
);
